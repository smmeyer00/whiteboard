/**
 * TODO: Create a floating button in bottom right that shows
 * when process.env.NODE_ENV !== 'production'
 * This button will show a modal with the current state, user info,
 * tldraw json, whatever else is usefull, etc...
 */
